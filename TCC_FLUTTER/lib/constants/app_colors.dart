import 'package:flutter/material.dart';

class AppColors {
  // Main colors
  static const Color background = Color(0xFF121212);
  static const Color cardBackground = Color(0xFF252525);
  static const Color accent = Colors.amber;
  
  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFBBBBBB);
  static const Color textMuted = Color(0xFF8A8A8A);
  
  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFE53935);
  
  // Filter chip colors
  static const Color chipBackground = Color(0xFF353535);
  static const Color chipSelectedBackground = Color(0xFFFFB300);
  static const Color chipText = Colors.white;
  static const Color chipSelectedText = Colors.black;
}